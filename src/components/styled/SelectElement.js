import styled from 'styled-components'

export default styled.select`
  height: 40px;
  font-weight: 300;
  border: 2px solid gray;
  border-radius: 5px;
  font-size: 18px;

  :focus {
    outline: 0;
  }
`
